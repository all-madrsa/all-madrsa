<!doctype html>
<html lang="hi">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Talimi Ittehad</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    .dropdown:hover .dropdown-menu { display:block; }
    .dropdown-menu { display:none; position:absolute; background:white; box-shadow:0 2px 6px rgba(0,0,0,0.2); border-radius:0.5rem; min-width:160px; z-index:50; }
    .dropdown-menu a { display:block; padding:6px 12px; font-size:14px; color:#333; }
    .dropdown-menu a:hover { background:#f3f4f6; }
    .btn-3d {
      background: linear-gradient(90deg,#ff4d4d,#4dffb8,#4d94ff,#d24dff);
      box-shadow: 0 4px 0 rgba(0,0,0,0.25), 0 6px 12px rgba(0,0,0,0.2);
      transition: transform .12s ease, box-shadow .12s ease;
      font-size: 0.75rem; padding: 0.25rem 0.6rem; color: white; font-weight: 600; border-radius: 6px;
    }
    .btn-3d:active { transform: translateY(1px); box-shadow: 0 2px 0 rgba(0,0,0,0.25); }
    marquee { background:#fff3cd; color:#856404; padding:6px; font-size:14px; border-bottom:1px solid #fcd34d; }
    .translate-btn {
      background: #ffffff;
      border: 2px solid #4f46e5;
      color: #4f46e5;
      font-weight: bold;
      padding: 4px 8px;
      border-radius: 50%;
      cursor: pointer;
      transition: 0.2s;
    }
    .translate-btn:hover { background:#4f46e5; color:#fff; }
  </style>
</head>
<body class="bg-slate-50 min-h-screen">

<!-- Header -->
<header id="header" class="bg-gradient-to-r from-purple-700 via-pink-600 to-yellow-500 shadow sticky top-0 z-40">
  <div class="max-w-6xl mx-auto flex flex-col px-3">
    <!-- Logo + Translate -->
    <div class="flex justify-center items-center gap-3 py-2 relative">
      <img id="siteLogo" src="default-logo.png" alt="Talimi Ittehad Logo" 
           class="h-10 sm:h-12 object-contain" />
      <!-- 🌐 Translate Button -->
      <button id="translateBtn" class="translate-btn">🌐</button>
      <div id="google_translate_element" class="hidden absolute mt-10 bg-white shadow rounded z-50"></div>
    </div>
    <!-- Menu -->
    <div class="flex flex-wrap justify-center items-center gap-2 pb-2 text-xs sm:text-sm">
      <div class="dropdown relative">
        <button class="btn-3d">About ⬇</button>
        <div class="dropdown-menu">
          <a href="#">हमारे बारे में</a>
          <a href="#">मिशन</a>
        </div>
      </div>
      <div class="dropdown relative">
        <button class="btn-3d">More ⬇</button>
        <div class="dropdown-menu">
          <a href="#">संपर्क करें</a>
          <a href="#">FAQ</a>
        </div>
      </div>
      <button id="addBtn" class="btn-3d hidden">+ Add Madarsa</button>
      <button id="authBtn" class="btn-3d">Login</button>
    </div>
  </div>
</header>

<!-- Notice Bar -->
<div id="noticeBar" class="w-full">
  <marquee id="noticeText">📢 Welcome to Talimi Ittehad!</marquee>
</div>
<div id="noticeAdmin" class="hidden max-w-6xl mx-auto px-4 mt-2">
  <textarea id="noticeInput" class="border p-2 w-full rounded text-sm" placeholder="Update notice text..."></textarea>
  <button id="saveNotice" class="btn-3d mt-1">💾 Save Notice</button>
</div>

<!-- Admin Logo Upload -->
<div id="logoAdmin" class="hidden max-w-6xl mx-auto px-4 mt-2">
  <label class="block text-sm text-gray-700">Update Site Logo</label>
  <input id="logoFile" type="file" accept="image/*" class="block mt-1"/>
  <button id="saveLogo" class="btn-3d mt-2">💾 Save Logo</button>
</div>

<!-- Search -->
<div class="max-w-6xl mx-auto px-4 mt-2 flex gap-2">
  <input id="searchBox" type="text" placeholder="🔍 Search Madarsa..." class="flex-1 border p-2 rounded"/>
</div>

<!-- Main -->
<main class="max-w-6xl mx-auto p-4">
  <div id="list" class="grid md:grid-cols-2 lg:grid-cols-3 gap-4"></div>
</main>

<!-- Modal -->
<div id="regModal" class="fixed inset-0 bg-black/40 hidden flex items-center justify-center p-4 z-50 overflow-auto">
  <div class="bg-white rounded-lg shadow max-w-xl w-full p-4">
    <div class="flex justify-between items-center">
      <h3 class="text-lg font-semibold text-emerald-600">Register Madarsa</h3>
      <button onclick="closeModal()" class="text-gray-600">X</button>
    </div>
    <form id="regForm" class="mt-3 space-y-3">
      <input id="mname" placeholder="Madarsa ka naam" class="w-full border p-2 rounded" required />
      <input id="maddress" placeholder="Address" class="w-full border p-2 rounded" required />
      <input id="mcontact" placeholder="Contact number" class="w-full border p-2 rounded" required />
      <input id="mwhatsapp" placeholder="WhatsApp number" class="w-full border p-2 rounded" />
      <input id="mcourses" placeholder="Courses" class="w-full border p-2 rounded" />
      <textarea id="mabout" placeholder="About Madarsa" rows="3" class="w-full border p-2 rounded"></textarea>
      <div>
        <label class="text-sm">Upload Images (1-5)</label>
        <input id="mimages" type="file" accept="image/*" multiple required />
      </div>
      <div>
        <label class="text-sm">Upload UPI QR</label>
        <input id="mupi" type="file" accept="image/*" />
      </div>
      <div class="flex justify-end gap-2">
        <button type="button" onclick="closeModal()" class="px-4 py-2 border rounded">Cancel</button>
        <button id="submitBtn" type="submit" class="btn-3d">Submit</button>
      </div>
    </form>
  </div>
</div>

<!-- Footer -->
<footer class="bg-gray-100 text-center py-4 mt-6">
  <p class="text-xs text-gray-500 mt-2">© 2025 Talimi Ittehad</p>
</footer>

<!-- Google Translate -->
<script type="text/javascript">
function googleTranslateElementInit() {
  new google.translate.TranslateElement({
    pageLanguage: 'hi',
    includedLanguages: 'hi,en,ur,ar',
    layout: google.translate.TranslateElement.InlineLayout.SIMPLE
  }, 'google_translate_element');
}
document.getElementById("translateBtn").addEventListener("click", () => {
  document.getElementById("google_translate_element").classList.toggle("hidden");
});
</script>
<script src="//translate.google.com/translate_a/element.js?cb=googleTranslateElementInit"></script>

<!-- Firebase + JS -->
<script type="module">
  import { initializeApp } from "https://www.gstatic.com/firebasejs/9.23.0/firebase-app.js";
  import { getAuth, GoogleAuthProvider, signInWithPopup, signOut, onAuthStateChanged } from "https://www.gstatic.com/firebasejs/9.23.0/firebase-auth.js";
  import { getFirestore, collection, addDoc, updateDoc, deleteDoc, doc, getDoc, setDoc, query, orderBy, onSnapshot, serverTimestamp } from "https://www.gstatic.com/firebasejs/9.23.0/firebase-firestore.js";

  const firebaseConfig = {
    apiKey: "AIzaSyDMvkPqFKDjrMaLlBkercBYoLxEs1GEbng",
    authDomain: "all-madrsa.firebaseapp.com",
    projectId: "all-madrsa",
    storageBucket: "all-madrsa.appspot.com",
    messagingSenderId: "619313835874",
    appId: "1:619313835874:web:e387e1297663f11fc2463b"
  };

  const app = initializeApp(firebaseConfig);
  const auth = getAuth(app);
  const db = getFirestore(app);
  const provider = new GoogleAuthProvider();

  const ADMIN_EMAIL = "zaradigitalseva@gmail.com";
  const IMGBB_API_KEY = "c4bc97c133edb76e062de6d070df67e4";

  const siteLogo = document.getElementById("siteLogo");
  const logoAdmin = document.getElementById("logoAdmin");
  const logoFile = document.getElementById("logoFile");
  const saveLogo = document.getElementById("saveLogo");

  // Load logo from Firestore
  async function loadLogo() {
    try {
      const snap = await getDoc(doc(db, "config", "logo"));
      if (snap.exists() && snap.data().logoUrl) {
        siteLogo.src = snap.data().logoUrl;
      }
    } catch (e) {
      console.log("Logo load error", e);
    }
  }

  // Upload image to ImgBB
  async function uploadToImgBB(file) {
    const form = new FormData();
    form.append("image", file);
    const res = await fetch(`https://api.imgbb.com/1/upload?key=${IMGBB_API_KEY}`, { method: "POST", body: form });
    const data = await res.json();
    return data.data.url;
  }

  // Save logo
  saveLogo.addEventListener("click", async () => {
    const file = logoFile.files[0];
    if (!file) { alert("Please select a logo image"); return; }
    try {
      const url = await uploadToImgBB(file);
      await setDoc(doc(db, "config", "logo"), { logoUrl: url });
      siteLogo.src = url;
      alert("✅ Logo updated!");
    } catch (e) {
      alert("❌ Error updating logo: " + e.message);
    }
  });

  // Auth
  onAuthStateChanged(auth, (user) => {
    if (user && user.email === ADMIN_EMAIL) {
      logoAdmin.classList.remove("hidden");
    } else {
      logoAdmin.classList.add("hidden");
    }
    loadLogo();
  });
</script>
</body>
</html>
